// Shared auth constants — imported by both client-side code and Edge middleware.
// Keep this file free of any browser/Node globals so it is safe in all runtimes.

/** localStorage key and cookie name for the JWT access token. */
export const AUTH_TOKEN_KEY = "educagent_access_token";
